读入图像

```
img = cv2.imread('Hemanvi.jpeg')
```

裁剪图像，转成灰度

```
img = img[50:250, 40:240]
img_gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)
```

转换图像大小

```
img_gray_small = cv2.resize(img_gray, (25,25))
```

----------

处理彩色图像

```
img = cv2.imread('Hemanvi.jpeg')

img = img[50:250,40:240,:]
img = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)
```

----------

```
data_folder = '~/data/FMNIST'
fmnist = datasets.FashionMNIST(data_folder, download=True, train=True)
```

```
tr_images = fmnist.data
tr_targets = fmnist.targets
```

```
print(tr_targets.unique())
# 一共十个类型
```

```
R,C = len(tr_targets.unique()), 10
fig, ax = plt.subplots(R,C,figsize=(10,10))
for label_class, plot_row in enumerate(ax):
    label_x_rows = np.where(tr_targets == label_class)[0]
    for plot_cell in plot_row:
        plot_cell.grid(False); plot_cell.axis('off')
        ix = np.random.choice(label_x_rows)
        x, y = tr_images[ix], tr_targets[ix]
        plot_cell.imshow(x, cmap='gray')
plot.tight_layout()
```

```
fmnist = datasets.FashionMNIST(data_folder,download=True,train=True)
tr_images=fmnist.data
tr_targets=fmnist.targets
```

```
class FMNISTDataset(Dataset):
    def __init__(self,x,y):
        x = x.float()
        x = x.view(-1,28*28) # 将每张图片扁平化
        self.x, self.y = x,y
    
    def __getitem__(self,ix):
        x,y = self.x[ix], self.y[ix]
        return x.to(device),y.to(device)
    
    def __len__(self):
        return len(self.x)
```

```
def get_data():
    train = FMNISTDataset(tr_images, tr_targets)
    trn_dl = DataLoader(train,batch_size=32,shuffle=True)
    return trn_dl
```

定义模型、损失函数、优化器

```
def get_model():
    model = nn.Sequential(
        nn.Linear(28*28, 1000),
        nn.ReLU(),
        nn.Linear(1000,10)
    ).to(device)
    loss_fn = nn.CrossEntropyLoss()
    optimizer = SGD(model.parameters(), lr=1e-2)
    return model, loss_fn, optimizer
```

定义一个函数，用于在图像的一个批处理上训练数据集

```
def train_batch(x, y, model, opt, loss_fn):
    model.train()
    prediction = model(x)
    batch_loss = loss_fn(prediction, y)
    batch_loss.backward()
    optimizer.step()
    optimizer.zero_grad()
    return batch_loss.item()
```

构建一个函数计算一个指定数据集的准确率

```
@torch.no_grad() # 明确表示不需要计算梯度
def accuracy(x,y,model):
    model.eval()
    prediction = model(x)
    max_values, argmaxes = prediction.max(-1)
    is_correct = argmaxes == y
    return is_correct.cpu().numpy().tolist()
```

使用以下代码行训练神经网络

初始化模型、损失函数、优化器和数据加载器

```
trn_dl = get_data()
model, loss_fn, optimizer = get_model()
```

初始化列表，这些列表包含了每个训练周期结束时的准确率和损失值

```
losses, accuracies = [], []
```

定义训练周期

```
for epoch in range(5):
    print(epoch)
```

初始化列表，这些列表包含了一个训练周期内每个批处理对应的准确率和损失值

```
epoch_losses, epoch_accuracies = [], []
```

通过遍历 DataLoader 创建训练数据的批处理：

```
for ix, batch in enumerate(iter(trn_dl)):
    x, y = batch
```

使用 train_batch 函数训练批处理，并在训练结束时将损失值作为 batch_loss 保存到批处理的顶部。

```
batch_loss = train_batch(x,y,model,optimizer,loss_fn)
epoch_losses.append(batch_loss)
```

存储一个训练周期内所有批处理的平均损失值

```
epoch_loss = np.array(epoch_losses).mean()
```

计算训练结束时所有批处理的预测准确率

```
for ix,batch in enumerate(iter(trn_dl)):
    x,y = batch
    is_correct = accuracy(x,y,model)
    epoch_accuracies.extend(is_correct)
epoch_accuracy = np.mean(epoch_accuracies)
```

在每个训练周期结束时，将损失值和准确率保存到一个列表中：

```
losses.append(epoch_loss)
accuracies.append(epoch_accuracy)
```

