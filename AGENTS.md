# AGENTS.md

## 项目定位

这是一个以 Markdown 为主的个人技术知识库仓库，不是常规的博客程序或前端应用仓库。

- 核心资产是技术笔记、专题目录、配套图片、抓包文件、PDF，以及少量示例代码和配置文件。
- 当前仓库里没有明显的 `package.json`、静态站点生成器配置或统一构建入口。默认应把这里理解为“内容源文件仓库”。
- 更新任务通常是补充知识点、修订表述、补图、补参考链接、补目录索引，而不是修改应用代码。

## 先看哪里

接到内容更新任务后，优先按这个顺序建立上下文：

1. 根目录 [README.md](README.md)
2. 目标主题目录下的 `README.md`（如果存在）
3. 目标目录中与当前主题最接近的现有文章
4. 目标文章引用的图片、抓包、PDF 等静态资源

如果某个主题目录存在 `README.md`，它通常承担“目录 / 索引 / 导航页”的角色，新增文章后通常要同步补链接。

## 目录地图

仓库主要按主题分区，内容密度最高的区域大致如下：

- `网络详解/`
  - 大体量专题目录，常见结构是 `chapter-xx/xx-yy.md`
  - [网络详解/README.md](网络详解/README.md) 是核心索引
- `操作系统详解/`
  - 大体量专题目录，结构稳定，常见命名是 `chapter-xx/xx-yy.md`
  - [操作系统详解/README.md](操作系统详解/README.md) 是核心索引
- `BasicClass/`
  - 偏基础课程、教材学习笔记、OS/算法/数据库/硬件等
  - 部分子目录含配套代码，例如 `BasicClass/OS/myOS/codes/`
- `工具/`
  - 各类工具、监控、nginx、redis、git、shell 等主题笔记
  - 有些目录同时带实验配置，如 `docker-compose.yml`、脚本等
- `语言/`
  - 各语言专题笔记，包含 `cpp`、`golang`、`python`、`rust`、`汇编语言` 等
- `传输协议/`
  - HTTP/HTTPS/KCP/FTP 等协议笔记，部分文章带抓包附件
- `k8s/`、`docker/`、`CICD/`、`音视频/`
  - 既有说明文档，也有示例 YAML、Dockerfile、脚本
- `static/`
  - 静态资源总目录
  - `static/images/`：文章配图，常按时间目录组织，如 `YYMM/`
  - `static/pcapng/`：抓包文件
  - `static/pdf/`：PDF 附件
  - `static/drawio/`：绘图源文件

## 内容组织规律

这个仓库的内容风格不是统一模板化的，更新时要“贴着现有风格写”，不要强行重构。

- 文件命名既有中文标题，也有英文标题，也有编号式文件名。
- 很多专题目录采用编号命名，例如 `chapter-01/01-05.md`。
- 也有大量直接用主题名命名的文章，例如 `kind-kindnet.md`、`TLS握手过程.md`。
- 不是所有文章都有一级标题；有些从 `##` 开始，有些开头就是图片或代码块。
- 没有统一 frontmatter，不要擅自为全仓库补 YAML frontmatter。
- 语言风格以中文为主，但经常夹杂英文术语、英文原文摘录或双语解释；应保留这种写法。
- 某些目录的 `README.md` 很简短，甚至只是一个备忘列表；不要因为“看起来不完整”就重写成统一模板，除非任务明确要求。

## 静态资源约定

现有文章大量使用根路径资源引用，常见形式如下：

```md
![](/static/images/2311/p003.png)
[抓包文件](/static/pcapng/2312/p001.pcap)
[参考 PDF](/static/pdf/2412/pcasm-book.pdf)
```

更新时遵循这些约定：

- 优先复用已有静态资源，不要重复上传同一内容。
- 新增图片时，沿用 `static/images/<时间目录>/pNNN.<ext>` 的现有命名风格。
- 新增抓包或 PDF 时，分别放到 `static/pcapng/`、`static/pdf/` 下的对应子目录。
- Markdown 中优先继续使用现有的 `/static/...` 根路径引用方式。
- 不要因为“想改成相对路径更规范”而批量改写旧文章的资源路径。

## 示例代码与配置

虽然仓库主体是文档，但有一部分目录包含真正的示例代码或实验配置，更新内容时要注意文档与示例的一致性。

常见情况包括：

- `BasicClass/OS/myOS/codes/` 下的 C 代码
- `docker/` 下的 `Dockerfile` 和构建脚本
- `k8s/`、`工具/minikube/`、`CICD/` 下的 YAML 示例
- 某些 `*-in-docker/` 目录下的 `docker-compose.yml`、脚本和配置文件

如果任务涉及这些目录：

- 先判断用户要改的是“说明文档”还是“配套示例”。
- 若改了示例配置，最好同步检查旁边的文档说明是否仍然准确。
- 若只是改笔记正文，不要顺手重构同目录示例代码。

## Shell 与路径注意事项

仓库路径包含大量中文、空格和特殊字符目录名，命令行操作时必须小心引用。

例如：

- `Linux IO/`
- `工具/监控/prometheus/prometheus client_golang使用.md`
- `音视频/协议-Dash|Cmaf/`

建议：

- 路径一律加单引号，例如 `sed -n '1,120p' '工具/监控/prometheus/prometheus client_golang使用.md'`
- 搜索优先用 `rg`
- 遍历目录时优先用 `find '目录名' ...`
- 不要假设路径可以不转义直接拼接
- 如果需要在 CUDA 设备上跑 `AI/codes/llm-train-cpp/`，远端 Windows 机器上的项目源码和构建目录放到 D 盘中，不要放到 C 盘临时目录。

## 更新内容时的默认工作流

1. 确定目标主题归属到哪个顶层目录。
2. 阅读该主题目录和相邻文章，先理解现有命名方式和写作粒度。
3. 判断应该“更新已有文章”还是“新增一篇文章”。
4. 如果新增文章，沿用该目录现有命名规则，不要引入新的编号体系。
5. 如果目录存在 `README.md` 索引，新增文章后补上入口链接。
6. 如果新增图片或附件，放到 `static/` 对应位置，并用现有路径风格引用。
7. 自查相关链接、图片路径、代码块和相邻目录引用是否一致。

## 适合 agent 做的事

- 补充已有文章内容
- 修正错别字、病句、失效表述
- 给专题新增一篇同风格笔记
- 为已有文章补目录入口
- 给说明文档补图片、代码示例、参考资料
- 统一某个小范围目录内的局部格式，但前提是该目录本身已经有明确风格

## 不要默认做的事

- 不要把仓库当成需要构建、测试、发布的应用项目
- 不要全局补 frontmatter、标签、摘要、日期等元信息
- 不要批量重命名文件以追求“统一命名规范”
- 不要批量把中文标题改成英文，或把英文内容强行翻译成中文
- 不要未经要求重排整个 `README.md` 或大目录索引
- 不要移动已有静态资源，只因为目录名字看起来“不够规范”

## 快速摸清上下文的命令

```bash
rg --files -g 'README.md'
rg -n '关键词' '目标目录'
find '目标目录' -maxdepth 2 -type f | sort
rg -n '/static/images|/static/pcapng|/static/pdf' -g '*.md' '目标目录'
git diff --stat
```

如果要判断某篇新文章是否应该进目录索引，优先搜索该目录 `README.md` 现有的链接组织方式，再按原样补充，不要自创结构。

## 成功标准

一次高质量的内容更新，通常满足以下条件：

- 放在了正确的主题目录下
- 命名方式与同目录文件一致
- 沿用了本地已有写作风格
- 静态资源路径正确
- 需要时同步更新了目录 `README.md`
- 没有引入无关的全局格式化或结构重写
